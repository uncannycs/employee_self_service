<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_task_form2_inherit_subtask" model="ir.ui.view">
        <field name="name">project.task.form.inherit.subtask</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">
            <!-- Add Smart Button for Sub-tasks in Backend -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_subtasks"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-tasks"
                        invisible="subtask_count == 0">
                    <field name="subtask_count" widget="statinfo" string="Sub-tasks"/>
                </button>
            </xpath>

            <!-- Add Sub-tasks notebook page in Backend -->
            <!-- <xpath expr="//notebook" position="inside">
                <page string="Sub-tasks" name="sub_tasks_page">
                    <field name="child_ids" context="{'default_parent_id': id, 'default_project_id': project_id}">
                        <list editable="bottom">
                            <field name="name"/>
                            <field name="user_ids" widget="many2many_tags"/>
                            <field name="stage_id"/>
                            <field name="date_deadline"/>
                            <field name="allocated_hours" widget="float_time" optional="show"/>
                            <field name="effective_hours" widget="float_time" optional="show"/>
                        </list>
                    </field>
                </page>
            </xpath> -->
        </field>
    </record>
</odoo>
